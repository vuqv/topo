<!-- Thin include so CHANGELOG.md stays the single source of truth (repo root).
     Each docs version renders its own CHANGELOG as it existed on that branch/tag. -->

```{include} ../CHANGELOG.md
```
