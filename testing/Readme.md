# Compare between Quyen and Yang implementation

This directory contains the code from Quyen, which is developed as python package named topo, which has been included in system Path so that it can be imported.

The original implementation is from Yang, in which he develop as standalone script which contains many steps to run the simulations.